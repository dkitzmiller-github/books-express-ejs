/*
export function initDb() {

    let auth = undefined;
    Author.create({
        name: "Thomas Dillon",
        genre: "Historical Fiction",
    }).then(author => {
            console.log(author);
            auth = author;
    }).catch(err => console.log(err));

    Book.create({
        title: "The Path Between the Seas",
        pages: 450,
    }).then(book => console.log(book))
      .catch((err) => console.log(err));

    Book.create({
        title: "Only the lonely die young",
        pages: 330,
    }).then(book => console.log(book))
        .catch((err) => console.log(err));
}

*/
